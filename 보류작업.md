# 보류 중인 작업

작업이 끝난 뒤 처리할 항목. 완료되면 지울 것.

---

## 1. 동영상 4개 YouTube 업로드 · 링크 연결 — **완료 (2026-09-07)**

원본 mp4 12개(1,466MB) 가운데 고유한 4개만 연구실 유튜브에 올리고
11개 글에 연결했다. 본문에 남아 있던 재생 안 되는 재생기 12개와
받을 수 없는 mp4 첨부 12건도 함께 정리했다.

| 영상 | 주소 | 들어간 글 |
|---|---|---|
| BK-LUPIC 3차년도 5월 출장 | https://youtu.be/58aoeInOeWs | `/social/209` `/en/social/100` |
| BK-LUPIC 1,2차년도 성과 | https://youtu.be/MwVLEdphjOw | `/news/59` `/en/news/26` `/social/209` |
| BK-LUPIC 초청연수 | https://youtu.be/KhdAd6S-4qU | `/news/63` `/en/news/28` `/ru/news/41` |
| BK-LUPIC 2024년 7월 출장 리포트 | https://youtu.be/IymQnHfKO7o | `/social/210` `/en/social/101` `/ky/social/151` `/ru/social/193` |

원본 mp4 는 `_수집자료/attach/` 에 그대로 남아 있다.

---
## 2. 25 MB 초과 PDF 1건 — **3번(자료실)과 함께 처리하기로 결정 (2026-09-07)**

`2025 INF 6th_proceeding_RUS.pdf` — 31.9 MB.
Cloudflare 정적 파일 한도(25 MB)를 넘어 지금은 이름만 회색으로 표시된다.

- 원본: `_수집자료/attach/news_ru/2025 INF 6th_proceeding_RUS.pdf`
- 붙어 있는 글: **`/ru/news/43`** «VI Международный форум по сестринскому делу»
- 새 사이트에서 기대하는 경로: `/files/news_ru/2025-inf-6th_proceeding_rus-a794d298.pdf`
- 같은 문서의 영문판(21.5 MB)은 한도 이내라 이미 올라가 있다

### 압축하지 않기로 한 이유

파일을 뜯어보니 **이미지 1,763개(JPEG 1,496개), 임베드 글꼴 0개** — 스캔 문서다.
글자까지 전부 그림이라 압축하면 글자가 번진다. 여는 데는 지장이 없지만 읽기 나빠진다.
이 컴퓨터에 압축 도구(Ghostscript 등)도 없어 새로 설치해야 한다.

그래서 **3번에서 R2 를 도입할 때 원본 그대로 올린다.** R2 에는 25 MB 제한이 없다.

### ⚠️ 처리할 때 반드시 지킬 것

이 PDF 는 **자료실이 아니라 공개 뉴스 게시판**에 붙어 있다.
자료실 파일과 같은 자물쇠를 채우면 **공개 글의 첨부가 갑자기 로그인을 요구하게 된다.**
R2 에 넣되 **회원 확인 없이 바로 내보내도록** 따로 처리할 것.

---

## 3. 자료실 회원제 (2단계)

**상태**: 설계 합의 + **자료 확보 완료** · 개발 착수 전

### 확보한 자료 (2026-09-07 수집, 실패 0건)

원본 자료실은 로그인해야 열리므로 관리자 세션으로 받았다.
받은 자리: `_수집자료/pds/` — **용량 때문에 GitHub 에 올리지 못했다. 이 컴퓨터에만 있다.**

| 항목 | 수량 |
|---|---|
| 글 | 97건 (러시아어판 `pds_ru` 만 실제 데이터, 한국어판은 시험글 1건, 영문·키르기즈어판은 비어 있음) |
| 첨부 | 148개 / 742.6 MB |
| └ 25MB 이하 | 140개 / 282.8 MB |
| └ 25MB 초과 | 8개 / 459.8 MB (포럼영상 6편, PDF 2개) |

### 만들 때 지켜야 할 것

- 파일을 `public/` 에 넣으면 **주소만 알면 누구나 받는다.** 회원제가 무의미해진다.
- 그래서 파일은 **R2**(10GB 무료)에 두고 **Worker** 가 로그인 확인 후 내보내야 한다.
- 글 97건도 같은 방식으로 `/library/*` 를 Worker 가 지켜야 한다.
- 회원 정보는 **D1**(무료)에 둔다.

### R2 용량 검토 (2026-09-07)

| 항목 | 용량 |
|---|---|
| 자료실 첨부 148개 | 742.6 MB |
| 공개 뉴스의 러시아어 PDF 1건 (2번 항목) | 31.9 MB |
| **합계** | **약 775 MB — 무료 한도 10 GB 의 7.8%** |

**증가 속도**: 자료실 97건이 2025-06-27 ~ 2026-06-15, 정확히 1년 치다.
연 약 740 MB → **10 GB 도달까지 약 13년.**

읽기·쓰기 횟수는 문제되지 않는다 (무료 한도 월 읽기 1천만·쓰기 100만 회).
**R2 는 전송량이 무료**라 트래픽 비용도 없다.
한도를 넘겨도 1 GB당 월 0.015달러라 5 GB 초과 시 월 100원 수준이다.

### 증가 속도를 줄이려면 — 영상을 유튜브로

| 구분 | 개수 | 용량 |
|---|---|---|
| 영상 (mp4) | 7개 | **332.4 MB (전체의 45%)** |
| 문서 | 141개 | 410.2 MB |

포럼영상 6편이 대부분이고 포럼이 매년 열리면 매년 이만큼 쌓인다.
영상을 유튜브로 빼면 **연 740 MB → 410 MB, 13년 → 24년**.

**다만 판단이 필요하다.** 자료실은 회원 전용인데 유튜브 '일부공개' 는
주소를 아는 사람이면 누구나 볼 수 있다. 검색에 걸리지 않고 대량 수집도 안 되지만
회원 제한보다는 느슨하다. 포럼영상은 공개 행사 녹화라 크게 민감하지 않아 보이나
**사업단이 정할 문제다. 2단계 착수 시점에 결정한다.**

### 보관 방식 — R2 로 정함 (2026-09-07)

| | R2 | 저장소에 넣기 |
|---|---|---|
| 25 MB 초과 파일 | **제한 없음** (121 MB PDF 도 원본 그대로) | 압축하거나 쪼개야 함 |
| 용량 한계 | 10 GB — 현재 속도로 13년 | GitHub 권장 1 GB — 약 2년 |
| 저장소 공개 여부 | 무관 | **비공개로 바꿔야 함** (지금 brightkyr-web 은 공개) |
| 이관 시 | 775 MB 복사 (1~2시간) | 없음 |
| 비용 | 0원 (초과해도 소액) | 0원 |

**R2 를 쓴다.** 이관 때 한두 시간 더 드는 것보다,
121 MB PDF 를 원본 그대로 보관할 수 있는 이점이 크다.

### 비용 — 사실상 0원

현재 두 사이트의 외주 호스팅 비용이 **연 80만원**이다. 이것을 대체한다.

| 저장 용량 | 연간 비용 |
|---|---|
| 775 MB (지금) | **0원** — 무료 한도 10 GB 의 7.8% |
| 10 GB (13년 뒤) | **0원** |
| 20 GB | 약 26,000원 |
| 50 GB | 약 13만원 |

R2 는 **전송량이 무료**라 방문자가 늘어도 저장 용량만 보면 된다.
D1(회원 정보)은 무료 한도 5 GB, 회원 27명이면 수 MB 수준이라 평생 무료.

### 착수 시점에 확인할 것 — R2 결제수단 등록 여부

R2 를 켤 때 무료 한도 안에서 쓰더라도 결제수단 등록을 요구할 수 있다.
(2026-09-07 기준 확인하지 못했다.) 화면을 보고 판단할 것.

- 요구하지 않으면 → 그대로 진행
- 요구하면 → 카드 등록 + **예산 알림 $1 설정** (청구 발생 시 즉시 메일)
  무료 한도 안이면 청구가 발생하지 않는다. 13년 치 여유가 있어 사실상 불가능하다.
- 그래도 꺼려지면 → 위 표의 '저장소에 넣기' 로 우회 (저장소를 비공개로 바꿀 것)

### 합의된 사항 (2026-09-07 갱신)

- 로그인 폼은 기존 홈페이지와 같은 형태 (아이디/비밀번호)
- 수집 항목에서 **생년월일·연락처 제외**
- **자가 가입 · 즉시 이용** — 승인제로 하지 않는다
  취지는 "최소한 가입이라는 문턱은 두자" 이지, 완전한 접근 통제가 아니다.
  **자가 가입인 이상 작정한 상대는 가입해서 받아갈 수 있다.** 기대치를 그렇게 잡을 것.
- 비밀번호 재설정은 관리자가 처리 (이메일 발송 없음)
- **기존 회원 27명은 이전하지 않고 다시 가입시킨다** (2026-09-07 결정)
  → 데이터 이전·비밀번호 일괄 재설정 작업이 통째로 없어졌다

### 보안문자 — Cloudflare Turnstile 사용

원본 백오피스 글쓰기 화면에 찌그러진 글자 보안문자가 있었다.
지금 기준으로는 문자 인식에 쉽게 뚫리고 사람만 불편하다.
대신 **Turnstile**(무료·무제한, Cloudflare 제공)을 쓴다.
정상 방문자는 대부분 아무 조작 없이 통과하고, 자동 가입은 막힌다.

| 화면 | 적용 |
|---|---|
| 회원가입 | 적용 |
| 로그인 | 비밀번호 여러 번 틀린 뒤에만 표시 |
| 관리자 `/admin` | 불필요 (GitHub 로그인이 훨씬 강하다) |

**주의**: Turnstile 은 사이트 키(공개)와 비밀 키(비공개) 한 쌍이 필요하다.
비밀 키는 Worker 시크릿으로 넣는데, **워커를 다시 배포하면 지워질 수 있다.**
(GITHUB_CLIENT_SECRET 이 실제로 그렇게 날아갔다. nursphere-new/README.md 참고)

### ⚠️ 개인정보처리방침을 함께 고쳐야 한다

현재 방침에 적힌 수집 항목이 실제와 다르다.

```
현재 표기 : 이름, 연락처, 이메일, 문의내용 등
실제      : 아이디, 비밀번호, 이름, 이메일  (연락처는 받지 않기로 함)
```

**방침과 실제가 다르면 그 자체가 문제가 된다.** 회원제를 만들 때 같이 고칠 것.
파일 위치: `public/sub/pop_privacy.html` 및 `public/{en,ky,ru}/sub/pop_privacy.html`

### 개인정보가 개인 계정에 쌓이지 않게 하는 순서

```
개발 → 테스트 계정으로 확인 → 컨펌 → 이관 → 그 다음에 안내·회원 가입 시작
```

회원이 이관 후에 가입하면 개인정보가 처음부터 연구실 계정에만 쌓인다.
개인 계정에는 테스트 계정 몇 개만 남고 그건 지우면 된다.

### 기존 회원 명단 — 데이터 이전용이 아니라 '알릴 대상' 으로 필요

재가입시키기로 했으므로 데이터는 필요 없다.
다만 **27명에게 "다시 가입하세요" 라고 알려야 하는데, 그 명단이 백오피스 안에만 있다.**
호스팅이 끊기면 같이 사라진다. **이메일 주소만이라도 복사해 둘 것.** (급하지는 않음)

### 자료실은 러시아어에만 둔다 (2026-09-07 결정)

자료 97건이 전부 러시아어이고, 한국팀이 자료실에서 받을 것은 사실상 없다.
원본도 러시아어판에만 실제 자료가 있었다(`pds_ru`).
한국어 메뉴에 걸어두면 열어본 사람만 헛걸음한다.

- 메뉴는 **러시아어판에만** 표시
- 관리자가 확인할 때는 주소로 직접 접근: `/ru/library`
- 영어·키르기즈어판은 원본과 같이 자료실 메뉴 없음

### ⚠️ 저장소를 비공개로 바꿔야 한다

자료실 글 97건의 본문을 저장소에 두면 `/admin` 에서 그대로 관리할 수 있다.
그런데 **`brightkyr-web` 은 지금 공개라 GitHub 에서 누구나 읽을 수 있다.**
회원제가 무의미해지므로 **자료실 착수 시 비공개로 전환할 것.**

- Cloudflare Workers Builds 는 비공개 저장소도 무료로 연결된다
- `/admin` 도 그대로 작동한다 (`nursphere-web` 이 이미 비공개다)
- 공개로 둬서 얻는 이점은 없다

첨부 파일은 어차피 R2 에 두므로 저장소에 들어가지 않는다.

### 같이 붙여야 할 것 — 헤더의 로그인 링크

원본 헤더에는 자료실이 있는 두 언어에만 로그인·회원가입 링크가 있다.

| 언어 | 링크 |
|---|---|
| 한국어 | 로그인 · 회원가입 |
| 러시아어 | Войти (Login) · Присоединиться (Join) |
| 영어 · 키르기즈어 | 없음 |

**지금은 넣지 않았다.** 갈 곳이 없어 깨진 링크가 되기 때문이다.
로그인 페이지를 만들 때 함께 붙인다.

현재 `/library` 는 안내 문구만 있는 자리표시 페이지다.
`robots.txt` 에서 이미 검색 노출을 막아 두었다.

---

## 4. 연구실 계정으로 이관 (2026-09-07 방침 결정)

**순서**: 본인 계정에서 완성 → 확인 → 사업단 컨펌 → **그 뒤에 이관**

컨펌 안 난 것을 미리 옮기면 헛수고가 될 수 있으므로 이 순서로 한다.
이관에 하루 정도 걸리는 것은 감수하기로 했다.

### 현재 소유 구조

| 항목 | 소유 | 비고 |
|---|---|---|
| GitHub `hyunmh3-oss` | 개인 | **조직이 아니라 개인 계정** (2026-09-04 생성) |
| └ `nursphere-web` | | 비공개 |
| └ `brightkyr-web` | | 공개 · 201 MB |
| └ `sveltia-cms-auth` | | 공개 |
| GitHub OAuth 앱 `NURSphere CMS` | `hyunmh3-oss` | Transfer ownership 버튼 있음 |
| Cloudflare 계정 | `Hyunmh3@naver.com` | 개인 |

`hyunmh3-oss` 가 **조직이 아니라 개인 계정**인 것이 핵심이다.
조직이면 사람만 바꿔 끼우면 되는데 개인 계정은 그게 안 된다.
**이관할 때 조직(Organization)을 새로 만들어 저장소를 옮기는 것이 정석이다.** 조직은 무료.

### 이관 절차

1. 연구실 GitHub **조직** 생성 (무료)
2. 저장소 3개 이전 (Settings → Transfer). 옛 주소는 자동으로 전달된다
3. OAuth 앱 이전 (Transfer ownership)
4. `public/admin/config.yml` 의 `repo:` 값을 새 주소로 수정 (두 사이트 모두)
5. 연구실 Cloudflare 계정 생성
6. Workers 프로젝트 3개를 새 계정에서 Git 연결 후 재배포
7. `GITHUB_CLIENT_SECRET` 다시 등록 (README 의 파일 방식 참고)
8. R2 버킷 내용 775 MB 복사, D1 내보내기 → 가져오기
9. 개인 계정에서 결제수단 제거

### 이메일만으로는 안 된다

GitHub·Cloudflare 모두 **계정을 새로 만들어야 한다.** 이메일은 계정 생성 재료일 뿐이다.
연구실이 이미 이 서비스 계정을 갖고 있을 가능성은 낮다.

| 방식 | 내용 |
|---|---|
| A. 연구실 이메일로 직접 만들고 넘기기 | `brightkyrgyzstan@khu.ac.kr` 메일함에 접근 가능하면 빠르다. 넘긴 뒤 비밀번호 변경·2단계인증 설정 필요 |
| B. 연구실 담당자가 직접 만들게 하기 | 처음부터 소유가 깔끔하다 |

---

## 5. 원본 사이트에 남아 있는 결함 (그대로 둔 것)

새 사이트에서 고치지 않고 원본 표기를 따랐다. 필요하면 나중에 정리.

| 위치 | 내용 |
|---|---|
| 한국어 메뉴 '자료실' | 원본은 **러시아어판 자료실**로 연결된다. 새 사이트는 `/library` 로 바로잡았다. |
| 키르기즈어 메뉴 '참여방법' | 원본 하위 항목이 **한국어판 주소**를 가리킨다. 새 사이트는 `/ky/join` 으로 바로잡았다. |
| 본문 속 도메인 오타 | 여러 글에 `brightkyrgystan.khu.ac.kr` (z 빠짐)로 적혀 있다. 글 내용이라 그대로 두었다. |
| 목록 '공지' 딱지 | 원본이 키르기즈어·러시아어 화면에서도 한국어 '공지'로 표시한다. |
| 슬라이더 버튼 | 원본이 모든 언어에서 '이전/다음'(한국어)으로 되어 있다. |
| 표 설명(caption) | 원본이 '뉴스 (영문) List' 처럼 한국어와 섞여 있다. 화면에는 보이지 않는다. |
| 키르기즈어 개인정보처리방침 팝업 | 제목만 한국어 '개인정보처리방침'이고 본문은 키르기즈어다. |
| `.sub-visual` CSS | 원본 CSS가 참조하는 `sv2~sv5.jpg` 등 16개 이미지가 원본 서버에도 없다. 실제로 쓰이지 않는 규칙이다. |

### 원본보다 낫게 바꾼 것

| 항목 | 원본 | 새 사이트 |
|---|---|---|
| 언어 전환 | 어느 페이지에서 눌러도 **그 언어의 첫 화면**으로 간다 | **보고 있던 페이지의 같은 언어판**으로 간다 |
| 목록 검색·쪽 넘김 | 서버(PHP)가 처리 | 화면에서 바로 처리 (주소가 바뀌지 않음) |
| 첨부파일 | 없는 파일도 링크가 걸려 눌리면 오류 | 파일이 없으면 이름만 회색으로 표시 |
| 글꼴·CSS | 외부 CDN(rawgit, jsdelivr, Google Fonts) 의존 | 전부 자체 보관 — 바깥 서비스가 멈춰도 화면이 안 깨진다 |
